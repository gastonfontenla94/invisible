# dependencias
node_modules/

# caché de Astro
.astro/

# NOTA: dist/ (el sitio ya construido) SÍ va al repositorio, a propósito.
# Cloudflare despliega esa carpeta tal cual, sin depender de poder
# construir el proyecto en su servidor.

# sistema
.DS_Store
Thumbs.db
desktop.ini

# entorno
.env
.env.production
*.log

# NOTA: src/data/ y public/img/ SÍ van al repositorio.
# Los genera el importador desde los CSV de Airtable, y Cloudflare
# los necesita para construir el sitio (no tiene acceso a tu OneDrive).
